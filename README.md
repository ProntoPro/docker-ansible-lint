# Docker image for [ansible-lint](https://github.com/willthames/ansible-lint)

### Build
`docker build -t prontopro/ansible-lint:TAG .`

### Usage
`docker run --rm -v $(pwd):/ansible prontopro/ansible-lint`
